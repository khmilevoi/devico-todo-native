import styled from 'styled-components/native';

export const TodosWrapper = styled.ScrollView`
  paddingTop: 50px;
`;

export const TodoWrapper = styled.View`
  padding: 5px 10px;
  display: flex;
  flexDirection: row;
`;

export const Inner = styled.Text``;
